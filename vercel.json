{
  "buildCommand": "npm install --omit=optional && npm run build",
  "installCommand": "npm install --omit=optional",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
