{
  "buildCommand": "npm install --no-optional && npm run build",
  "installCommand": "npm install --no-optional",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
